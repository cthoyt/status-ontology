# Project Status Ontology

A model of the lifecycle of a project/resource.

Convert from OBO to OWL with `just export`. For now, the OWL artifact isn't version controlled to make clear the single
source of truth is the `ontology.obo` file.
